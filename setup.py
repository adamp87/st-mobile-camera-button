from setuptools import setup, find_packages

setup(
   name='st_mobile_camera_button',
   version='0.1',
   description='',
   author='AdamP',
   packages=find_packages(),
   include_package_data=True,
   python_requires='>=3.10',
)